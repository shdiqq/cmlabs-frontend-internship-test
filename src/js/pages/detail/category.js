const Detail_Category = {
  async init() {
    await this._initialData();
    this._initialListener();
  },

  async _initialData() {

  },

  _initialListener() {

  },
}

export default Detail_Category;